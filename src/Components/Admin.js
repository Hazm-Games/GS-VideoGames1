import React from "react";

const Admin = () => {

 /*   useEffect(() => {
        fetch(`/api/admin`)
          .then((response) => response.json())
          .then() => {
           set();
           
          });
      }, []); */
    return (
  <div>
  <h1>ADMIN PAGE</h1>

   </div>
    )
}

export default Admin